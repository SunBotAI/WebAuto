
# 智谱 AI 配额抢购脚本 - 技术方案

## 1. 概述

本文档描述「按顺序点汉字」验证码绕过脚本的技术方案，用于智谱 AI coding plan 配额抢购。

## 2. 验证码类型分析

### 2.1 目标验证码
- **类型**：文字点选（按顺序点击汉字）
- **常见供应商**：极验（Geetest）、腾讯防水墙、网易易盾
- **特点**：
  - 给出 4-6 个汉字
  - 要求按指定顺序点击
  - 有背景干扰

### 2.2 技术挑战
1. **汉字识别**：准确识别验证码图片中的汉字
2. **位置定位**：确定每个汉字在图片中的坐标
3. **顺序识别**：识别需要点击的汉字顺序
4. **行为伪装**：模拟真人点击轨迹
5. **反检测**：避免被识别为机器人

## 3. 技术选型

### 3.1 OCR 方案
| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| PaddleOCR | 开源、中文识别准确率高 | 需要安装 paddlepaddle | ⭐⭐⭐⭐⭐ |
| EasyOCR | 轻量、安装简单 | 中文识别准确率一般 | ⭐⭐⭐ |
| 自训 YOLO | 精准定位 | 需要收集数据训练 | ⭐⭐ |

**推荐**：PaddleOCR + OpenCV 预处理

### 3.2 浏览器自动化
| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| Playwright | 现代 API、自动等待、多浏览器支持 | 相对新 | ⭐⭐⭐⭐⭐ |
| Selenium | 成熟、生态丰富 | 需要 webdriver | ⭐⭐⭐ |
| puppeteer | Chrome 原生支持 | 仅 Chrome | ⭐⭐⭐ |

**推荐**：Playwright

### 3.3 反检测方案
- Playwright 自带反检测
- 指纹伪装（User-Agent、WebGL、Canvas）
- 鼠标轨迹模拟（贝塞尔曲线、随机加速度）

## 4. 系统架构

```
webauto/
├── scripts/
│   ├── main.py              # 主入口
│   ├── captcha_solver.py    # 验证码识别核心
│   ├── ocr_engine.py        # OCR 引擎
│   ├── browser_controller.py # 浏览器控制
│   └── humanizer.py         # 行为伪装
├── data/
│   ├── models/              # OCR 模型
│   └── templates/           # 验证码模板
├── tests/
│   └── test_captcha.py      # 单元测试
└── demo/
    ├── screenshots/         # demo 截图
    └── videos/              # demo 录屏
```

## 5. 核心模块设计

### 5.1 验证码识别流程
1. 截取验证码图片
2. 图像预处理（去噪、二值化）
3. OCR 识别汉字和位置
4. 识别需要点击的顺序
5. 生成点击坐标序列

### 5.2 行为伪装
- 鼠标轨迹：使用贝塞尔曲线生成自然轨迹
- 点击前停留：随机 100-500ms
- 点击加速度：模拟真人点击速度变化
- 误点修正：偶尔模拟误点再修正

### 5.3 反检测策略
- 禁用自动化特征
- 随机化 User-Agent
- 模拟人类操作间隔（2-5 秒）
- Canvas/WebGL 指纹随机化

## 6. 测试方案

### 6.1 测试目标
使用公开验证码 demo 页面验证技术可行性：
- 极验 demo：https://www.geetest.com/demo/
- 腾讯防水墙 demo：https://007.qq.com/

### 6.2 验收标准
- 验证码识别准确率 &gt; 90%
- 单次验证时间 &lt; 10 秒
- 连续成功次数 &gt; 10 次

## 7. 风险与应对

| 风险 | 影响 | 概率 | 应对方案 |
|------|------|------|----------|
| 验证码样式变更 | 高 | 中 | 模块化设计，快速适配 |
| 账号被封 | 高 | 低 | 控制频率，使用小号测试 |
| OCR 识别失败 | 中 | 中 | 多 OCR 引擎 fallback |
| 反检测失效 | 高 | 低 | 持续优化反检测策略 |

## 8. 开发计划

1. **阶段 1**：环境搭建 + OCR 选型测试
2. **阶段 2**：验证码识别核心开发
3. **阶段 3**：浏览器自动化 + 行为伪装
4. **阶段 4**：Demo 验证 + 优化

## 9. 技术栈

- **语言**：Python 3.10+
- **浏览器自动化**：Playwright
- **OCR**：PaddleOCR
- **图像处理**：OpenCV + Pillow
- **反检测**：playwright-stealth
