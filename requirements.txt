# WebAuto - Web 页面自动化框架核心依赖

# ========== 浏览器自动化 ==========
playwright>=1.40.0

# ========== HTTP 客户端 ==========
httpx[http2]>=0.25.0
brotli>=1.0.0  # 解 br 压缩(部分站点默认返回,如 example.com)

# ========== HTML 解析 ==========
lxml>=4.9.0
cssselect>=1.2.0

# ========== OCR / 验证码识别 ==========
# 方案 1: PP-OCRv6 (推荐，中文识别准确率高)
paddlepaddle>=2.5.0
paddleocr>=2.7.0

# 方案 2: ddddocr (轻量备选)
ddddocr>=1.4.11

# ========== 时间同步 ==========
ntplib>=0.4.0

# ========== 图像处理 ==========
Pillow>=10.0.0
numpy>=1.24.0

# ========== 系统工具 ==========
psutil>=5.9.0

# ========== Web 服务（可选）==========
# 智谱凭证管理面板使用 (Tools/credential_panel.py)
# 锁定 6.x:gradio 4.x + starlette 1.3.1 + fastapi 0.139 时间线不一致(gradio 4.44 是 2024-09,
# starlette 1.3 是 2025-06,fastapi 0.139 是 2026-07),触发 jinja2 模板 unhashable dict 错误。
# gradio 6.x 是 2026 年发布,与 fastapi 0.139 同年代,兼容性已验证(HTTP 200)。
gradio>=6.0,<7.0
# 后端 API 能力（如要做 REST 服务化时启用，目前 Tools/ 用 gradio 就够）
# fastapi>=0.104.0
# uvicorn>=0.24.0
